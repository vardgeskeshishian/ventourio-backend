<!DOCTYPE html>
<html>
    <head>
        <title>{{ $title }}</title>
        <style>
            @page { margin:0px; }
        </style>
    </head>
    <body>
        <img src="{{ $image['src'] }}" alt="{{ $image['alt'] }}" style="height: 100%; width: 100%">
    </body>
</html>
